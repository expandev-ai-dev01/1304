import { Router } from 'express';

const router = Router();

// Internal routes will be added here as features are implemented
// Example:
// import productController from '@/api/v1/internal/product/controller';
// router.get('/product', productController.listHandler);
// router.post('/product', productController.createHandler);

export default router;
